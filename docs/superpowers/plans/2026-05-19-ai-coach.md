# AI Coach & Home Screen Redesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a rules-based AI coach layer that reads the last 7 days of sessions and rides, recommends today's workout, flags exercises needing modification, offers safe swaps, and surfaces this via a redesigned home card and a new pre-start review screen.

**Architecture:** `computeCoachRecommendation(sessions, rides, override)` is a pure derived value computed in `App()` and passed as a prop. The pre-start screen is a new `'preStart'` view state between the home Start button and the existing energy check. Coach modification notes are rendered ephemerally via props — nothing coach-related is stored in sessions or localStorage.

**Tech Stack:** React 18 (UMD), plain JSX, `src/IronLog.jsx` is the only file edited, build via `npm run build`, no test framework — verification is build success + manual smoke test in browser.

---

## Read before starting

- `CLAUDE.md` — canonical session guide, file path, build rules
- `docs/superpowers/specs/2026-05-19-ai-coach-design.md` — the spec (R3 version)
- `src/IronLog.jsx` — the only file you edit

**Never edit `dist/index.html` directly — always run `npm run build`.**

---

## File map

All changes are in `src/IronLog.jsx`. No new files.

| Location | Change |
|---|---|
| After `weekStart()` (~line 625) | Add `weekMondayStart()` helper + `computeCoachRecommendation()` |
| `buildSession()` (~line 545) | Add `preStartSwaps` param, dedupe, `phase: 'energy'` return |
| `startSession()` inside `ActiveWorkout` (~line 1168) | Preserve pre-built session; skip `buildSession()` if session exists |
| Before `Dashboard` (~line 722) | Add `PreStartScreen` component |
| `Dashboard` (~line 724) | Refactor: unified card, coach panel, flag pills, Why? expand, week strip |
| `ActiveWorkout` (~line 1126) | Add `coachRec` prop; render coach modification note near `def.caution` block |
| `App()` (~line 2961) | Add `showWhy`, `preStartSwaps` state; add `coachRec` derived value; add `'preStart'` view |

---

## Task 1 — `weekMondayStart()` + `computeCoachRecommendation()`

**Files:**
- Modify: `src/IronLog.jsx` — insert after `weekStart()` at line 625

The existing `weekStart()` returns Sunday as day 0. The week strip shows Mon–Sun, so a separate helper is needed. `computeCoachRecommendation` is a pure function — no React, no side effects.

- [ ] **Step 1: Insert the two functions after `weekStart()` (after line 625)**

Find this line (the closing brace of `weekStart`):
```js
function weekStart() {
  const d = new Date(); d.setHours(0,0,0,0);
  d.setDate(d.getDate() - d.getDay());
  return d;
}
```

Insert immediately after it:
```js
function weekMondayStart() {
  const d = new Date(); d.setHours(0,0,0,0);
  d.setDate(d.getDate() - ((d.getDay() + 6) % 7));
  return d;
}

function computeCoachRecommendation(sessions, rides, override = null) {
  const SWAP_MAP = {
    rdl:           { name: 'Hip Thrust',              id: 'hip_thrust' },
    kb_deadlift:   { name: 'Hip Thrust',              id: 'hip_thrust' },
    p_bb_row:      { name: 'Chest-Supported DB Row',  id: 'cs_db_row' },
    chin_up:       { name: 'Band Row',                id: 'band_row' },
    goblet_squat:  { name: 'Reverse Lunge',           id: 'reverse_lunge' },
  };
  const HINGE_IDS = new Set(['rdl', 'kb_deadlift']);

  const completed = [...sessions]
    .filter(s => s.completed)
    .sort((a, b) => new Date(a.date) - new Date(b.date));
  const last = completed[completed.length - 1] || null;

  // Determine workout
  const workout = override || (last ? ({ A: 'B', B: 'C', C: 'A' }[last.workout] || 'A') : 'A');
  const wkt = WORKOUTS[workout];

  const reasons = [];
  const noteFragments = [];
  const flags = [];

  // Signal: days since last session
  if (last) {
    const daysSince = Math.floor((Date.now() - new Date(last.date)) / 86400000);
    if (daysSince >= 4) {
      noteFragments.push(`${daysSince} days since your last session.`);
      reasons.push(`Last session was ${daysSince} days ago — ease in, RPE 6 max for first two exercises.`);
    }
  }

  // Signal: average RPE last session
  if (last) {
    const doneSets = (last.exercises || []).flatMap(ex => (ex.sets || []).filter(s => s.done));
    const rpeVals = doneSets.map(s => Number(s.rpe)).filter(v => !isNaN(v) && v > 0);
    if (rpeVals.length > 0) {
      const avg = rpeVals.reduce((a, b) => a + b, 0) / rpeVals.length;
      if (avg >= 8) {
        noteFragments.push('High effort last session.');
        reasons.push(`Average RPE was ${avg.toFixed(1)} last session — train at RPE 6–7 today.`);
      }
    }
  }

  // Signal: ride within last 48 hours
  const now = Date.now();
  const recentRide = rides.some(r => (now - new Date(r.date).getTime()) < 48 * 3600000);
  if (recentRide) {
    reasons.push('Rode in the last 48 hours — keep hinge movements light.');
    wkt.exercises.forEach(exId => {
      if (!HINGE_IDS.has(exId)) return;
      if (flags.some(f => f.exerciseId === exId)) return;
      const def = EXERCISES[exId] || PRESET_LIBRARY[exId];
      const sw = SWAP_MAP[exId];
      flags.push({
        exerciseId: exId,
        exerciseName: def?.name || exId,
        modification: 'Rode recently — keep weight raised and light. Prioritise pull movements.',
        swap: sw?.name || null,
        swapId: sw?.id || null,
      });
    });
  }

  // Signal: pain >= 3 on any workout exercise last session
  if (last) {
    (last.exercises || []).forEach(ex => {
      if (!wkt.exercises.includes(ex.id)) return;
      if (flags.some(f => f.exerciseId === ex.id)) return;
      const hasPain = (ex.sets || []).some(s => Number(s.pain) >= 3);
      if (!hasPain) return;
      const def = EXERCISES[ex.id] || PRESET_LIBRARY[ex.id];
      const sw = SWAP_MAP[ex.id];
      flags.push({
        exerciseId: ex.id,
        exerciseName: def?.name || ex.id,
        modification: 'Pain logged last session — keep weight conservative, increase range only if pain-free.',
        swap: sw?.name || null,
        swapId: sw?.id || null,
      });
      reasons.push(`Pain ≥ 3 logged on ${def?.name || ex.id} last session.`);
    });
  }

  // Compose note
  let note;
  if (noteFragments.length === 0 && flags.length === 0) {
    note = 'Good recovery. Train at your planned intensity today.';
  } else if (noteFragments.length > 0 && flags.length > 0) {
    note = noteFragments.join(' ') + ' Some exercises flagged — see adjustments below.';
  } else if (noteFragments.length > 0) {
    note = noteFragments.join(' ') + ' Take it steady today.';
  } else {
    note = 'All signals normal. Some exercises flagged below — review before starting.';
  }

  if (reasons.length === 0) reasons.push('No flags from the last 7 days. Good to go.');

  return { workout, headline: wkt.title, note, reasons, flags };
}
```

- [ ] **Step 2: Build to verify no syntax errors**

```bash
cd "/Users/phillcantone/Library/Mobile Documents/com~apple~CloudDocs/Family/Phill/AI Coding/Ironlog"
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
cd "/Users/phillcantone/Library/Mobile Documents/com~apple~CloudDocs/Family/Phill/AI Coding/Ironlog"
git add src/IronLog.jsx
git commit -m "feat: add computeCoachRecommendation and weekMondayStart helpers"
git push origin main
```

---

## Task 2 — Update `buildSession()`

**Files:**
- Modify: `src/IronLog.jsx:545–581`

Add a `preStartSwaps` parameter, apply swaps before building the exercise list, deduplicate exercise IDs, and add `phase: 'energy'` to the returned object.

- [ ] **Step 1: Replace `buildSession()` (lines 545–581)**

Replace this entire function:
```js
function buildSession(workoutKey, prevSessions, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}) {
  const wkt = WORKOUTS[workoutKey];
  const extraIds = workoutCustom[workoutKey] || [];
  const hiddenIds = new Set(workoutHidden[workoutKey] || []);
  const exerciseIds = [...wkt.exercises, ...extraIds].filter(id => !hiddenIds.has(id));
  const exercises = exerciseIds.map(exId => {
    const def = allExercises[exId] || EXERCISES[exId];
    if (!def) return null;
    const last = getLastLogged(prevSessions, exId);
    const numSets = last?.sets?.length || def.defaultSets;
    const sets = Array.from({ length: numSets }, (_, i) => {
      const ls = last?.sets?.[i] || last?.sets?.[0];
      const base = {
        weight: ls?.weight ?? '',
        reps: ls?.reps ?? (def.defaultReps || ''),
        duration: ls?.duration ?? (def.defaultDuration || ''),
        rpe: null, pain: null, done: false,
      };
      if (def.pullupTracking) {
        base.mode = ls?.mode ?? 'bw';
        base.band = ls?.band ?? '';
      }
      return base;
    });
    return { id: exId, sets, notes: '' };
  }).filter(Boolean);
  return {
    id: Date.now().toString(),
    workout: workoutKey,
    date: new Date().toISOString(),
    startTime: Date.now(),
    energy: null,
    exercises,
    notes: '',
    completed: false,
  };
}
```

With this updated version:
```js
function buildSession(workoutKey, prevSessions, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}, preStartSwaps = {}) {
  const wkt = WORKOUTS[workoutKey];
  const extraIds = workoutCustom[workoutKey] || [];
  const hiddenIds = new Set(workoutHidden[workoutKey] || []);
  // Apply swaps: replace flagged IDs with their chosen replacements
  const rawIds = [...wkt.exercises, ...extraIds].filter(id => !hiddenIds.has(id));
  const swappedIds = rawIds.map(id => preStartSwaps[id] || id);
  // Deduplicate (swap target may already appear in custom extras)
  const exerciseIds = [...new Set(swappedIds)];
  const exercises = exerciseIds.map(exId => {
    const def = allExercises[exId] || EXERCISES[exId];
    if (!def) return null;
    const last = getLastLogged(prevSessions, exId);
    const numSets = last?.sets?.length || def.defaultSets;
    const sets = Array.from({ length: numSets }, (_, i) => {
      const ls = last?.sets?.[i] || last?.sets?.[0];
      const base = {
        weight: ls?.weight ?? '',
        reps: ls?.reps ?? (def.defaultReps || ''),
        duration: ls?.duration ?? (def.defaultDuration || ''),
        rpe: null, pain: null, done: false,
      };
      if (def.pullupTracking) {
        base.mode = ls?.mode ?? 'bw';
        base.band = ls?.band ?? '';
      }
      return base;
    });
    return { id: exId, sets, notes: '' };
  }).filter(Boolean);
  return {
    id: Date.now().toString(),
    workout: workoutKey,
    date: new Date().toISOString(),
    startTime: Date.now(),
    energy: null,
    exercises,
    notes: '',
    completed: false,
    phase: 'energy',
  };
}
```

- [ ] **Step 2: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
git add src/IronLog.jsx
git commit -m "feat: buildSession accepts preStartSwaps, dedupes, returns phase:energy"
git push origin main
```

---

## Task 3 — Update `startSession()` inside `ActiveWorkout`

**Files:**
- Modify: `src/IronLog.jsx:1168–1175`

When the pre-start flow pre-builds a session and sets it as `activeSession`, `startSession()` must use it rather than rebuild from scratch.

- [ ] **Step 1: Replace `startSession()` (lines 1168–1175)**

Replace:
```js
  function startSession(energy) {
    const s = buildSession(nextWkt, sessions, allExercises, workoutCustom, workoutHidden);
    s.energy = energy;
    setSession(s);
    setActiveSession(s);
    setPhase('warmup');
    setActiveSession(s => s ? { ...s, phase: 'warmup' } : s);
  }
```

With:
```js
  function startSession(energy) {
    // If activeSession was pre-built by the pre-start screen (has exercises), preserve it.
    // Only add the energy level and advance to warmup — do not rebuild.
    const base = (activeSession?.exercises?.length > 0)
      ? activeSession
      : buildSession(nextWkt, sessions, allExercises, workoutCustom, workoutHidden);
    const s = { ...base, energy, phase: 'warmup' };
    setSession(s);
    setActiveSession(s);
    setPhase('warmup');
  }
```

- [ ] **Step 2: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
git add src/IronLog.jsx
git commit -m "feat: startSession preserves pre-built session from pre-start screen"
git push origin main
```

---

## Task 4 — `PreStartScreen` component

**Files:**
- Modify: `src/IronLog.jsx` — insert the new component immediately before the `Dashboard` component (before line 721: `// ═══ DASHBOARD ═══`)

This component shows today's flagged exercise modifications, allows swapping individual exercises, and launches the session.

- [ ] **Step 1: Insert `PreStartScreen` before the Dashboard section divider**

Find this comment:
```js
// ═══════════════════════════════════════════════════════════════════════
// DASHBOARD
// ═══════════════════════════════════════════════════════════════════════
```

Insert the following component immediately before it:

```js
// ═══════════════════════════════════════════════════════════════════════
// PRE-START SCREEN
// ═══════════════════════════════════════════════════════════════════════
function PreStartScreen({ selectedWorkout, coachRec, preStartSwaps, setPreStartSwaps, allExercises, sessions, workoutCustom, workoutHidden, setActiveSession, setView }) {
  const wkt = WORKOUTS[selectedWorkout];
  const extraIds = workoutCustom[selectedWorkout] || [];
  const hiddenIds = new Set((workoutHidden || {})[selectedWorkout] || []);
  const exerciseCount = [...wkt.exercises, ...extraIds].filter(id => !hiddenIds.has(id)).length;

  function handleStartSession() {
    const session = buildSession(selectedWorkout, sessions, allExercises, workoutCustom, workoutHidden, preStartSwaps);
    setActiveSession(session);
    setPreStartSwaps({});
    setView('workout');
  }

  function toggleSwap(flag) {
    setPreStartSwaps(prev => {
      if (prev[flag.exerciseId]) {
        const next = { ...prev };
        delete next[flag.exerciseId];
        return next;
      }
      return { ...prev, [flag.exerciseId]: flag.swapId };
    });
  }

  return (
    <div style={{ padding: '16px 16px 100px', minHeight: '100vh', background: C.bg }}>
      {/* Back */}
      <button onClick={() => setView('dashboard')} style={{ background: 'none', border: 'none', color: C.muted, cursor: 'pointer', fontSize: 13, fontFamily: C.fMono, padding: 0, marginBottom: 20, display: 'block' }}>
        ← Back
      </button>

      {/* Workout badge */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 24 }}>
        <div style={{ fontSize: 52, fontFamily: C.fDisplay, fontWeight: 700, color: C.amber, lineHeight: 1, flexShrink: 0 }}>
          {selectedWorkout}
        </div>
        <div>
          <div style={{ fontFamily: C.fDisplay, fontSize: 20, textTransform: 'uppercase', fontWeight: 700, letterSpacing: 0.5 }}>{wkt.title}</div>
          <div style={{ fontSize: 12, color: C.muted, fontFamily: C.fMono, marginTop: 3 }}>{exerciseCount} exercises</div>
        </div>
      </div>

      {/* Flags or all-clear */}
      {coachRec.flags.length === 0 ? (
        <div style={{ ...st.card(), borderLeft: `3px solid ${C.green}`, marginBottom: 20 }}>
          <div style={{ display: 'flex', gap: 10, alignItems: 'flex-start' }}>
            <span style={{ fontSize: 18 }}>✓</span>
            <div>
              <div style={{ fontFamily: C.fDisplay, fontSize: 14, textTransform: 'uppercase', color: C.green, letterSpacing: 1, marginBottom: 4 }}>All clear</div>
              <div style={{ fontSize: 13, color: C.muted, lineHeight: 1.5 }}>Good recovery since last session. No modifications needed.</div>
            </div>
          </div>
        </div>
      ) : (
        <div style={{ marginBottom: 20 }}>
          <div style={{ ...st.label, marginBottom: 10 }}>Today's adjustments</div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
            {coachRec.flags.map(flag => {
              const isSwapped = !!preStartSwaps[flag.exerciseId];
              const swapDef = isSwapped ? (allExercises[flag.swapId] || EXERCISES[flag.swapId]) : null;
              return (
                <div key={flag.exerciseId} style={{ ...st.card(), borderLeft: `3px solid ${isSwapped ? C.blue : C.amber}` }}>
                  <div style={{ fontSize: 14, fontWeight: 700, fontFamily: C.fDisplay, textTransform: 'uppercase', marginBottom: 8 }}>
                    {flag.exerciseName}
                  </div>
                  {isSwapped ? (
                    <div>
                      <div style={{ fontSize: 13, color: C.blue, marginBottom: 8 }}>⇄ Swapped → {swapDef?.name || flag.swap}</div>
                      <button onClick={() => toggleSwap(flag)} style={{ ...st.btnSm(C.dim, C.muted), fontSize: 11 }}>↩ Undo swap</button>
                    </div>
                  ) : (
                    <div>
                      <div style={{ fontSize: 12, color: C.amber, lineHeight: 1.5, marginBottom: flag.swapId ? 10 : 0 }}>
                        ⚠️ {flag.modification}
                      </div>
                      {flag.swapId && (
                        <button onClick={() => toggleSwap(flag)} style={{ ...st.btnSm(C.dim, C.muted), fontSize: 12 }}>
                          ⇄ Swap it out → {flag.swap}
                        </button>
                      )}
                    </div>
                  )}
                </div>
              );
            })}
          </div>
        </div>
      )}

      {/* Start button */}
      <button style={{ ...st.btn(), display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 8 }} onClick={handleStartSession}>
        <Icon name="play" size={16} /> Start Session
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
git add src/IronLog.jsx
git commit -m "feat: add PreStartScreen component"
git push origin main
```

---

## Task 5 — App() state, `coachRec`, and `'preStart'` view

**Files:**
- Modify: `src/IronLog.jsx:2961–3145` (App component)

Add `showWhy` and `preStartSwaps` state, compute `coachRec`, add the `'preStart'` view case, and update props passed to `Dashboard` and `ActiveWorkout`.

- [ ] **Step 1: Add `showWhy` and `preStartSwaps` state**

Find these lines in `App()`:
```js
  const [driveSync, setDriveSync] = useState({ status: 'idle', lastSync: null, error: null });
  const [updateAvailable, setUpdateAvailable] = useState(false);
```

Add two lines immediately after them:
```js
  const [showWhy, setShowWhy] = useState(false);
  const [preStartSwaps, setPreStartSwaps] = useState({});
```

- [ ] **Step 2: Add `coachRec` derived value**

Find this line:
```js
  const allExercises = { ...EXERCISES, ...PRESET_LIBRARY, ...customExercises };
```

Add the line immediately after it:
```js
  const coachRec = computeCoachRecommendation(sessions, rides, selectedWorkout);
```

- [ ] **Step 3: Add `'preStart'` view case in the JSX render**

Find this block in the JSX:
```js
        {view === 'dashboard' && (
          <Dashboard sessions={sessions} rides={rides} setView={setView} activeSession={activeSession}
```

Insert the following immediately before it:
```js
        {view === 'preStart' && (
          <PreStartScreen
            selectedWorkout={selectedWorkout}
            coachRec={coachRec}
            preStartSwaps={preStartSwaps}
            setPreStartSwaps={setPreStartSwaps}
            allExercises={allExercises}
            sessions={sessions}
            workoutCustom={workoutCustom}
            workoutHidden={workoutHidden}
            setActiveSession={setActiveSession}
            setView={setView}
          />
        )}
```

- [ ] **Step 4: Add `coachRec`, `showWhy`, `setShowWhy` props to the `Dashboard` call**

Find the Dashboard render call:
```js
        {view === 'dashboard' && (
          <Dashboard sessions={sessions} rides={rides} setView={setView} activeSession={activeSession}
            selectedWorkout={selectedWorkout} setSelectedWorkout={setSelectedWorkout}
            allExercises={allExercises} workoutCustom={workoutCustom}
            workoutHidden={workoutHidden} setWorkoutHidden={setWorkoutHidden}
            driveSync={driveSync} onCloudSync={handleCloudSync}
            updateAvailable={updateAvailable} onWarmupOpen={setWarmupDemoItem}
            onDemoOpen={setDemoExId} />
        )}
```

Replace with:
```js
        {view === 'dashboard' && (
          <Dashboard sessions={sessions} rides={rides} setView={setView} activeSession={activeSession}
            selectedWorkout={selectedWorkout} setSelectedWorkout={setSelectedWorkout}
            allExercises={allExercises} workoutCustom={workoutCustom}
            workoutHidden={workoutHidden} setWorkoutHidden={setWorkoutHidden}
            driveSync={driveSync} onCloudSync={handleCloudSync}
            updateAvailable={updateAvailable} onWarmupOpen={setWarmupDemoItem}
            onDemoOpen={setDemoExId}
            coachRec={coachRec} showWhy={showWhy} setShowWhy={setShowWhy} />
        )}
```

- [ ] **Step 5: Add `coachRec` prop to the `ActiveWorkout` call**

Find the ActiveWorkout render call:
```js
        {view === 'workout' && (
          <ActiveWorkout
            sessions={sessions}
            activeSession={activeSession}
            setActiveSession={setActiveSession}
            onComplete={handleComplete}
            setView={setView}
            selectedWorkout={selectedWorkout}
            allExercises={allExercises}
            workoutCustom={workoutCustom}
            workoutHidden={workoutHidden}
            setWorkoutHidden={setWorkoutHidden}
            onDemoOpen={setDemoExId}
            onWarmupOpen={setWarmupDemoItem}
          />
        )}
```

Replace with:
```js
        {view === 'workout' && (
          <ActiveWorkout
            sessions={sessions}
            activeSession={activeSession}
            setActiveSession={setActiveSession}
            onComplete={handleComplete}
            setView={setView}
            selectedWorkout={selectedWorkout}
            allExercises={allExercises}
            workoutCustom={workoutCustom}
            workoutHidden={workoutHidden}
            setWorkoutHidden={setWorkoutHidden}
            onDemoOpen={setDemoExId}
            onWarmupOpen={setWarmupDemoItem}
            coachRec={coachRec}
          />
        )}
```

- [ ] **Step 6: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 7: Commit**

```bash
git add src/IronLog.jsx
git commit -m "feat: App state + coachRec + preStart view wired up"
git push origin main
```

---

## Task 6 — Coach modification note in `ActiveWorkout`

**Files:**
- Modify: `src/IronLog.jsx:1126` (ActiveWorkout function signature)
- Modify: `src/IronLog.jsx:1441–1446` (caution banner block, inside workout phase view)

Add `coachRec` to the ActiveWorkout signature, then render a second banner for the coach modification note alongside the existing `def.caution` banner.

- [ ] **Step 1: Add `coachRec` to `ActiveWorkout` signature**

Find:
```js
function ActiveWorkout({ sessions, activeSession, setActiveSession, onComplete, setView, selectedWorkout, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}, onDemoOpen, onWarmupOpen }) {
```

Replace with:
```js
function ActiveWorkout({ sessions, activeSession, setActiveSession, onComplete, setView, selectedWorkout, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}, onDemoOpen, onWarmupOpen, coachRec }) {
```

- [ ] **Step 2: Add coach flag rendering after the `def.caution` block**

Find the caution banner block (around line 1441):
```js
        {/* Caution banner */}
        {def.caution && (
          <div style={{ display: 'flex', alignItems: 'flex-start', gap: 10, background: C.amber + '18', border: `1px solid ${C.amber}66`, borderRadius: 10, padding: '10px 14px', marginBottom: 14 }}>
            <span style={{ fontSize: 16, lineHeight: 1.4, flexShrink: 0 }}>⚠️</span>
            <div style={{ fontSize: 12, color: C.amber, lineHeight: 1.5, fontFamily: C.fMono }}>{def.caution}</div>
          </div>
        )}
```

Replace with:
```js
        {/* Caution banner */}
        {def.caution && (
          <div style={{ display: 'flex', alignItems: 'flex-start', gap: 10, background: C.amber + '18', border: `1px solid ${C.amber}66`, borderRadius: 10, padding: '10px 14px', marginBottom: 14 }}>
            <span style={{ fontSize: 16, lineHeight: 1.4, flexShrink: 0 }}>⚠️</span>
            <div style={{ fontSize: 12, color: C.amber, lineHeight: 1.5, fontFamily: C.fMono }}>{def.caution}</div>
          </div>
        )}
        {/* Coach modification note (ephemeral — from coachRec props, not stored in session) */}
        {coachRec?.flags?.find(f => f.exerciseId === exId)?.modification && (
          <div style={{ display: 'flex', alignItems: 'flex-start', gap: 10, background: C.amber + '18', border: `1px solid ${C.amber}66`, borderRadius: 10, padding: '10px 14px', marginBottom: 14 }}>
            <span style={{ fontSize: 16, lineHeight: 1.4, flexShrink: 0 }}>🤖</span>
            <div style={{ fontSize: 12, color: C.amber, lineHeight: 1.5, fontFamily: C.fMono }}>
              Coach: {coachRec.flags.find(f => f.exerciseId === exId).modification}
            </div>
          </div>
        )}
```

- [ ] **Step 3: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 4: Commit**

```bash
git add src/IronLog.jsx
git commit -m "feat: render ephemeral coach modification note in ActiveWorkout"
git push origin main
```

---

## Task 7 — Dashboard refactor

**Files:**
- Modify: `src/IronLog.jsx:724–879` (Dashboard component)

This is the largest change. Add `coachRec`, `showWhy`, `setShowWhy` to the Dashboard signature. Add coach note panel with Why? expand and flag pills to the unified card. Change the Start button to navigate to `'preStart'` instead of `'workout'`. Add the 7-day week strip below the unified card.

- [ ] **Step 1: Update `Dashboard` function signature**

Find:
```js
function Dashboard({ sessions, rides, setView, activeSession, selectedWorkout, setSelectedWorkout, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}, driveSync, onCloudSync, updateAvailable, onWarmupOpen, onDemoOpen }) {
```

Replace with:
```js
function Dashboard({ sessions, rides, setView, activeSession, selectedWorkout, setSelectedWorkout, allExercises = EXERCISES, workoutCustom = {}, workoutHidden = {}, driveSync, onCloudSync, updateAvailable, onWarmupOpen, onDemoOpen, coachRec, showWhy, setShowWhy }) {
```

- [ ] **Step 2: Add the week strip helper inside Dashboard**

Find this line inside Dashboard (it will be near the top of the function body):
```js
  const ws = weekStart();
  const weekSessions = sessions.filter(s => new Date(s.date) >= ws);
  const weekRides = rides.filter(r => new Date(r.date) >= ws);
```

Replace with:
```js
  const ws = weekStart();
  const weekSessions = sessions.filter(s => new Date(s.date) >= ws);
  const weekRides = rides.filter(r => new Date(r.date) >= ws);
  // Week strip: Mon–Sun cells
  const mondayStart = weekMondayStart();
  const weekDays = Array.from({ length: 7 }, (_, i) => {
    const d = new Date(mondayStart);
    d.setDate(mondayStart.getDate() + i);
    const dateStr = d.toISOString().slice(0, 10);
    const sess = sessions.find(s => s.completed && s.date?.slice(0, 10) === dateStr);
    const ride = rides.find(r => r.date?.slice(0, 10) === dateStr);
    const isToday = dateStr === new Date().toISOString().slice(0, 10);
    return { d, dateStr, sess, ride, isToday, dayLetter: ['M','T','W','T','F','S','S'][i] };
  });
```

- [ ] **Step 3: Add flag pills and coach note panel inside the unified workout card**

Find the workout hero card's compact summary row and Start button area. Specifically, find the Start button:
```js
        <button style={{ ...st.btn(), display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 8 }} onClick={() => setView('workout')}>
          <Icon name="play" size={16} /> {activeSession ? 'Resume Workout' : `Start Workout ${selectedWorkout}`}
        </button>
```

Replace with:
```js
        {/* Flag pills */}
        {!activeSession && coachRec?.flags?.length > 0 && (
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginBottom: 12 }}>
            {coachRec.flags.map(flag => (
              <span key={flag.exerciseId} style={{ background: C.amber + '18', color: C.amber, border: `1px solid ${C.amber}44`, borderRadius: 20, padding: '3px 10px', fontSize: 11, fontFamily: C.fMono }}>
                ⚠ {flag.exerciseName}
              </span>
            ))}
          </div>
        )}

        {/* Coach note panel */}
        {!activeSession && coachRec && (
          <div style={{ background: '#F0F4FF', borderRadius: 8, padding: '12px 14px', marginBottom: 14 }}>
            <div style={{ display: 'flex', alignItems: 'flex-start', gap: 8, marginBottom: showWhy ? 8 : 0 }}>
              <span style={{ fontSize: 15, flexShrink: 0 }}>🤖</span>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 13, color: '#2a3f6f', lineHeight: 1.5 }}>{coachRec.note}</div>
                <div style={{ display: 'flex', gap: 12, marginTop: 6 }}>
                  <button onClick={() => setShowWhy(v => !v)} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 0, fontSize: 12, color: '#4A82E8', fontFamily: C.fMono }}>
                    {showWhy ? '✕ Hide' : 'Why? ›'}
                  </button>
                  {coachRec.flags.length > 0 && (
                    <button onClick={() => setView('preStart')} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 0, fontSize: 12, color: C.amber, fontFamily: C.fMono }}>
                      Safe swaps ›
                    </button>
                  )}
                </div>
              </div>
            </div>
            {showWhy && (
              <div style={{ borderTop: '1px solid #d0daff', paddingTop: 10, marginTop: 2 }}>
                {coachRec.reasons.map((r, i) => (
                  <div key={i} style={{ fontSize: 12, color: '#4a5a7a', lineHeight: 1.5, marginBottom: 4, paddingLeft: 8 }}>• {r}</div>
                ))}
              </div>
            )}
          </div>
        )}

        <button style={{ ...st.btn(), display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 8 }}
          onClick={() => activeSession ? setView('workout') : setView('preStart')}>
          <Icon name="play" size={16} /> {activeSession ? 'Resume Workout' : `Start Workout ${selectedWorkout}`}
        </button>
```

- [ ] **Step 4: Add the week strip after the unified workout card**

Find the stretch routine card:
```js
      {/* Stretch Routine card */}
      <div style={{ ...st.card(), marginBottom: 16 }}>
```

Insert the week strip immediately before it:
```js
      {/* 7-day week strip */}
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 4, marginBottom: 12 }}>
        {weekDays.map(({ dayLetter, sess, ride, isToday }) => (
          <div key={dayLetter + Math.random()} style={{
            ...st.card(),
            padding: '8px 4px',
            textAlign: 'center',
            borderColor: isToday ? C.amber : C.border,
            background: isToday ? C.amberDim : C.card,
            display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 3,
          }}>
            <div style={{ ...st.label, fontSize: 9 }}>{dayLetter}</div>
            {sess ? (
              <div style={{ fontSize: 11, fontFamily: C.fDisplay, color: C.amber, lineHeight: 1 }}>{sess.workout}✓</div>
            ) : ride ? (
              <div style={{ fontSize: 12, lineHeight: 1 }}>🚴</div>
            ) : (
              <div style={{ fontSize: 11, color: C.muted, lineHeight: 1 }}>–</div>
            )}
          </div>
        ))}
      </div>

      {/* Stretch Routine card */}
```

Note: The `key` uses `Math.random()` as a workaround since day letters can repeat (two Tuesdays won't occur in a 7-day strip, but Thu/Tue both start with T). A better key is the `dateStr`. Update to use `dateStr`:

```js
      {/* 7-day week strip */}
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 4, marginBottom: 12 }}>
        {weekDays.map(({ dayLetter, sess, ride, isToday, dateStr }) => (
          <div key={dateStr} style={{
            ...st.card(),
            padding: '8px 4px',
            textAlign: 'center',
            borderColor: isToday ? C.amber : C.border,
            background: isToday ? C.amberDim : C.card,
            display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 3,
          }}>
            <div style={{ ...st.label, fontSize: 9 }}>{dayLetter}</div>
            {sess ? (
              <div style={{ fontSize: 11, fontFamily: C.fDisplay, color: C.amber, lineHeight: 1 }}>{sess.workout}✓</div>
            ) : ride ? (
              <div style={{ fontSize: 12, lineHeight: 1 }}>🚴</div>
            ) : (
              <div style={{ fontSize: 11, color: C.muted, lineHeight: 1 }}>–</div>
            )}
          </div>
        ))}
      </div>
```

- [ ] **Step 5: Build to verify**

```bash
npm run build
```

Expected: build completes with no errors.

- [ ] **Step 6: Smoke test in browser**

Open `dist/index.html` in a browser (or deploy and open on iPhone). Verify:
- Home screen shows coach note panel with 🤖 icon and note text
- "Why? ›" expands and shows reasons bullets, "✕ Hide" collapses it
- "Safe swaps ›" appears when flags exist, tapping goes to pre-start screen
- Start Workout button goes to pre-start screen (not directly to energy check)
- Pre-start screen shows flags or all-clear card
- Swap toggle works (amber → blue, swap name shown, undo works)
- Tapping Start Session enters the energy check normally
- Workout proceeds normally after selecting energy
- No coach data visible in History (completed session cards should be unaffected)
- Week strip shows Mon–Sun with correct workout letters and today highlighted

- [ ] **Step 7: Commit**

```bash
git add src/IronLog.jsx dist/index.html version.json
git commit -m "feat: Dashboard refactor — unified card, coach panel, flag pills, Why? expand, week strip"
git push origin main
```

---

## Task 8 — CHANGELOG and ROADMAP update

- [ ] **Step 1: Add entry to CHANGELOG.md**

Open `CHANGELOG.md` and add a new entry at the top (reverse-chronological):

```markdown
## 2026-05-19 — AI Coach & Home Screen Redesign

- Added `computeCoachRecommendation(sessions, rides, override)` rules engine
- Reads last 7 days: pain ≥ 3, avg RPE ≥ 8, ride within 48h, days since last session ≥ 4
- New `'preStart'` view — review modifications and optionally swap exercises before session begins
- Safe swaps: rdl → hip_thrust, kb_deadlift → hip_thrust, p_bb_row → cs_db_row, chin_up → band_row, goblet_squat → reverse_lunge
- Coach modification notes rendered ephemerally via props in ActiveWorkout — not stored in sessions
- Dashboard unified card: coach note panel (🤖), flag pills, Why? expand, Safe Swaps link
- New 7-day week strip (Mon–Sun) below unified card
- `buildSession()` updated: accepts preStartSwaps, deduplicates exercise list, returns phase:'energy'
- `startSession()` updated: preserves pre-built session from pre-start screen
```

- [ ] **Step 2: Update ROADMAP.md**

Mark the AI Coach feature as complete in ROADMAP.md. Find the relevant entry and mark it done.

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md ROADMAP.md
git commit -m "docs: mark AI Coach complete in CHANGELOG and ROADMAP"
git push origin main
```

---

## Self-review checklist

| Spec requirement | Task |
|---|---|
| `computeCoachRecommendation(sessions, rides, override)` | Task 1 |
| Sort sessions ascending before reading last | Task 1 (`.sort((a,b) => new Date(a.date)-new Date(b.date))`) |
| Pain ≥ 3 signal | Task 1 |
| Avg RPE ≥ 8 signal | Task 1 |
| Ride within 48h signal + hinge flag | Task 1 |
| Days since last session ≥ 4 signal | Task 1 |
| `CoachRec` object shape | Task 1 |
| Monday-based week strip helper | Task 1 |
| `buildSession()` preStartSwaps param | Task 2 |
| `buildSession()` deduplication | Task 2 |
| `buildSession()` returns `phase:'energy'` | Task 2 |
| `startSession()` preserves pre-built session | Task 3 |
| `PreStartScreen` component | Task 4 |
| Flag rows with swap toggle | Task 4 |
| All-clear card when no flags | Task 4 |
| App state: `showWhy`, `preStartSwaps` | Task 5 |
| `coachRec` as derived value in App | Task 5 |
| `'preStart'` view state | Task 5 |
| Props wired to Dashboard and ActiveWorkout | Task 5 |
| Coach modification note in ActiveWorkout (ephemeral) | Task 6 |
| Never stored in session or il_sessions | Task 6 (prop-only, not in session object) |
| Unified card with coach note panel | Task 7 |
| Flag pills | Task 7 |
| Why? expand / collapse | Task 7 |
| Safe swaps link → preStart | Task 7 |
| Start button → preStart (not directly to workout) | Task 7 |
| 7-day week strip Mon–Sun | Task 7 |
| Today highlighted | Task 7 |
| CHANGELOG updated | Task 8 |
