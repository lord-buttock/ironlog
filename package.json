{
  "name": "ironlog",
  "version": "1.0.0",
  "description": "Personal fitness tracking app — IronLog",
  "scripts": {
    "build": "node build.js",
    "watch": "nodemon --watch src/IronLog.jsx --exec 'node build.js'"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "@babel/preset-react": "^7.24.0",
    "nodemon": "^3.1.0"
  }
}
